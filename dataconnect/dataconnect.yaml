specVersion: "v1beta"
serviceId: "votex"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "votex-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
